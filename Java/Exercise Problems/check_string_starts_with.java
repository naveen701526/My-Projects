string = "w3resource.com"
print(string.startswith("w3r"))

  
  ////or we can do like this also
  
  str="w3resource.com"
  strStart="w3c"
  print(strStart.equals(str.substring(0,strStart.length())));
